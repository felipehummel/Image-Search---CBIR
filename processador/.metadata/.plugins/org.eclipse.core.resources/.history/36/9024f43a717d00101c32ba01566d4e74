class ImageScore {
		public int id;
		public float score;
		
		public ImageScore(int _id, float _score) {
			id = _id;
			score = _score;
		}
		
		@Override
		public String toString() {
			return id + " : "+score
		}
}